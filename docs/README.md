# Project website

This folder is the static GitHub Pages website for **Continuum Robot Modeling with Action Conditioned Flow Matching**.

Preview locally from the repository root:

```bash
python -m http.server 8000 -d docs
```

Deploy using GitHub repository **Settings → Pages → Deploy from a branch → main /docs**.
